{
  "name": "luma-watch",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node scripts/check.mjs",
    "watch": "node scripts/watch.mjs",
    "test": "node --test scripts/lib/*.test.mjs"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "nodemailer": "^6.9.16",
    "playwright": "^1.62.1"
  },
  "engines": {
    "node": ">=20"
  }
}
