-- 追い打ちの時刻を「リマインドからの経過時間」で持つ。
--
-- これまで最終の追い打ちだけ 23:30 固定だったため、リマインド時刻を
-- 変えても追随せず、22:00以降に設定すると1回目の追い打ちが
-- 1日の範囲(23:59)を超えて永久に飛ばない状態になっていた。
--
-- NULL は「最終の追い打ちを送らない」を意味する。
ALTER TABLE users ADD COLUMN final_nudge_after_min INTEGER DEFAULT 180;
